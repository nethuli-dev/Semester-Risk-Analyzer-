# Frontend

React 19 + Vite + Tailwind CSS v4. See the [root README](../README.md) for setup, scripts, and architecture.

```bash
npm run dev     # http://localhost:5173 (expects the API on :5050)
npm run build   # production build into dist/
```
